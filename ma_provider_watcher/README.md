# MA Provider Watcher

This add-on restores the `ytmusic_free` provider after Music Assistant updates and also backs up its provider configuration from `settings.json` so the provider can come back without manual re-entry of cookies.
